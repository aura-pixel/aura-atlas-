import { FormularioRegistro } from "@/components/auth/formulario-registro";

export default function Home() {
  return (
    <main className="min-h-screen flex items-center justify-center bg-slate-background">
      <FormularioRegistro />
    </main>
  );
}